name: Build and Deploy Java Application

on:
  push:
    branches:
      - master

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Cache Docker layers
        uses: actions/cache@v2
        with:
          path: /tmp/.buildx-cache
          key: ${{ runner.os }}-buildx-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-buildx-

      - name: Login to DockerHub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push Docker image
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: anaima/marriopizza:latest # Replace 'anaima' with your DockerHub username
          cache-from: type=local,src=/tmp/.buildx-cache
          cache-to: type=local,dest=/tmp/.buildx-cache

      # - name: Deploy to Server
      #   run: |
      #     # Add your deployment commands here
      #     # For example, SSH to your server and pull the new Docker image
      #     # ssh user@server "docker pull anaima/marriopizza:latest && docker run -d --name marriopizza -p 80:8080 anaima/marriopizza:latest"
      #   env:
      #     DEPLOY_KEY: ${{ secrets.DEPLOY_KEY }}
